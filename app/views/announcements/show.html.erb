<p>
  <strong>Text:</strong>
  <%= @announcement.text %>
</p>

<p>
  <strong>Expires at:</strong>
  <%= @announcement.expires_at.strftime('%m-%d-%y') %>
</p>

<%= link_to 'Edit', edit_announcement_path(id: @announcement.id) if can?(:manage, Announcement) %>|
<%= link_to 'Back', announcements_path %>
