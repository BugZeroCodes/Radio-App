<h2>Readings</h2>
<table class='table'>
  <tr>
    <th>Recited at</th>
    <th colspan="3">Configuration</th>
  </tr>

  <% @readings.each do |reading| %>
    <tr>
      <td><%= reading.recited_at.strftime('%m-%d-%Y') %></td>
      <td><%= link_to 'Show', announcement %></td>
      <td><%= link_to 'Edit', edit_announcement_path(id: announcement.id) %></td>
      <td><%= link_to 'Delete', announcement_path(id: announcement.id), method: :delete %></td>
    </tr>
  <% end %>
</table>
